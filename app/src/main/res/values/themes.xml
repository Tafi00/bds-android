<resources>
    <style name="Theme.FutaLand" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">#F7F9FC</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">#FFFFFF</item>
        <item name="android:windowLightNavigationBar">true</item>
        <!-- The app is light-only: never let the system apply "force dark" on top
             of it when the device is in dark mode. -->
        <item name="android:forceDarkAllowed">false</item>
    </style>
</resources>
